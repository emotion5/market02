# 작업 이력

> 보고용 변경 이력입니다. 최신 날짜가 위로 옵니다.
> 보고할 때는 해당 **날짜 블록만 복사**해서 사용하면 됩니다.
> (기술 커밋 메시지는 `git log` 참고 — 이 문서는 사용자에게 보이는 변화 중심)

---

## 2026-07-21

**관리자 — 상품정보제공고시 항목 추가**
- 상품 등록/수정에 **전자상거래 상품정보제공고시** 입력 추가 (모델명·제조국·제조자·크기규격·재질·색상·구성품·인증정보 8개, 모두 선택)
- 상품 상세페이지의 "크기" 자리(준비중)를 **"상품정보제공고시" 표**로 교체 — 미입력 항목은 "상세페이지 참조", 인증은 "해당없음"으로 표시하고 품명·A/S·품질보증·배송/교환은 상품명·사이트설정으로 자동 채움
- 고시 항목은 비워도 상품 등록에 지장 없음(운영자가 나중에 보완 가능)

**프론트 — 견적서 패널·헤더 UI 정리**
- 좌측 **견적서 패널 항목을 슬림하게** (제목+옵션 한 줄, 제거(×) 버튼을 우측 상단 구석으로) → 같은 높이에 더 많은 항목 표시
- **로고를 상단 고정**하고 그 아래로 견적서가 채워지며 **"견적서 보기 / 전체 비우기" 버튼은 화면 하단 고정**
- 헤더의 견적서 아이콘과 썸네일의 하트(견적서 추가) 아이콘을 **장바구니(카트) 아이콘으로 통일**

**결제 — 포트원 → 토스페이먼츠 직접 연동 전환**
- 가상계좌(무통장입금) 결제를 **포트원 경유에서 토스페이먼츠 직접 연동**으로 전환 (중간 수수료 절감 목적)
- 체크아웃이 **토스 결제창(리다이렉트)** 방식으로 바뀌고, 결제 후 전용 가상계좌·입금 안내 화면을 표시
- 입금 시 **자동 입금확인(웹훅)** 구조는 그대로 유지 (관리자 수동 입금확인도 가능)
- 포트원 관련 연동은 모두 제거
- ⚠️ **남은 작업**: 토스 개발자센터 전용 키 발급·웹훅 등록 후 **실제 결제 흐름 테스트**가 필요 (현재는 코드/빌드 검증까지만 완료)

**관리자 — 카테고리 추가·편집·순서변경 기능**
- 관리자 카테고리 화면에서 **카테고리를 직접 추가/삭제**하고 **표시명(한글·영문)** 을 수정할 수 있음 (기존엔 노출 토글만 가능)
- **▲▼ 버튼 + "순서 저장"** 으로 내비/홈 진열 순서를 조정
- slug(내부 키)은 상품·URL·이미지 폴더와 연결돼 **생성 후 변경 불가**, **소속 상품이 있는 카테고리는 삭제 차단**
- 카테고리가 **DB 기준으로 일원화** — 추가한 카테고리가 상품 등록 화면과 손님 페이지(카테고리·상품상세)에 **즉시 반영**됨 (기존 하드코딩 목록 의존 제거)

**견적서 — 품목별 썸네일 표시(경량)**
- 견적서 품목표의 각 품목명 옆에 **작은 상품 썸네일**을 표시 (작성 중 견적서·발행본·인쇄본 공통)
- 좌측/모바일 **장바구니(견적서) 패널** 썸네일도 같은 경량 이미지로 전환
- 원본 이미지 대신 **경량 썸네일(.thumb.webp, 약 4KB)** 을 불러 표시 — 상품 이미지 1장당 **약 25배(100KB→4KB)** 전송량 절감
- 썸네일이 아직 없는 과거 항목은 **자동으로 원본 이미지로 대체 표시**되어 깨지지 않음
- 상품 이미지 업로드 시 썸네일이 **자동 생성**되며, 기존 이미지는 `npm run thumbs` 로 일괄 생성

**정책 페이지 3종 신설 (PG 심사 대비)**
- **이용약관**(`/terms`), **개인정보처리방침**(`/privacy`), **환불정책**(`/refund`) 페이지 추가
- 전자상거래법 표준 항목 기반, 상호·대표자·연락처 등은 관리자 사이트설정 값과 자동 연동
- 푸터 하단에 세 페이지 링크 노출 (개인정보처리방침 강조)

**푸터 — 사업자 정보 표기(PG 심사 대비)**
- 하단 푸터에 **상호·대표자·사업자등록번호·주소·전화·이메일·개인정보보호책임자** 표시 (관리자 사이트설정 값 자동 연동)
- 사업자등록번호 옆 **공정위 '사업자정보확인' 링크** 추가
- **통신판매업신고번호는 "준비중"** 자리표시 (신고 후 실제 번호로 교체 예정)
- 어드민 화면 및 인쇄물에는 푸터 미노출

**견적서 인쇄 — 상단 헤더 숨김**
- 견적서를 인쇄/PDF 저장할 때 상단에 찍히던 **로고·견적서/계정/로그아웃 아이콘 바를 숨김** — 인쇄물에는 견적서 문서만 나오도록 정리

**상품 이미지 업로드 — 자동 최적화**
- 관리자가 이미지를 올리면 **긴 변 1600px로 자동 축소 + WebP 변환**해 저장 (원본이 크거나 무거워도 자동 처리 — 관리자가 미리 줄일 필요 없음)
- 폰 사진 **회전(EXIF) 자동 보정**, 1600px보다 작은 이미지는 억지로 확대하지 않음
- 저장·전송 용량을 상한으로 통제하면서 상세 확대 화질은 유지

---

## 2026-07-20

**관리자 — 카테고리 노출 설정(내비 / 메인 개별 토글)**
- 관리자에 **'카테고리 노출'** 화면 신설 — 카테고리마다 **상단 내비게이션 노출**과 **메인(홈) 진열대 노출**을 체크박스로 각각 켜고 끔
- 두 값이 독립이라 조합이 자유로움: 둘 다 노출(기본) / 내비만 / **내비는 끄고 홈에만 노출**(예: 벽지). 홈만 노출인 카테고리를 혹시 내비에도 켜두면 그 칩은 카테고리 전용 목록 페이지로 연결됨
- 기존에는 카테고리 목록이 코드에 하드코딩돼 있어 노출을 바꾸려면 개발이 필요했음 — 이제 화면에서 저장하면 즉시 반영. 좌측 메뉴 '상품 관리' 아래에 추가
- 최초 값은 **모두 노출**로 시작해 기존 화면과 동일

**무통장입금 자동화 — 포트원 가상계좌 연동**
- 체크아웃 결제를 **고정계좌 안내 → 주문별 전용 가상계좌 발급**으로 전환(포트원 V2, 토스페이먼츠 채널)
- 고객이 발급 계좌로 **입금하면 자동으로 "입금확인"** 처리(포트원 웹훅 → 서명검증 → 금액대조 → 자동 확정). 관리자 수동 확인 버튼은 안전망으로 유지
- 입금기한(24시간) 경과·결제실패 시 주문 **자동 취소**(주문상태에 '주문취소' 추가)
- 완료 화면에 발급 계좌번호·입금기한 표시
- (현재 테스트 채널 연동 — 실서비스 전환은 포트원 실계정 키로 교체만 하면 됨)

**사업자등록증 첨부 — 실제 업로드 연동**
- 사업자회원 가입 시 첨부한 **사업자등록증 파일이 실제로 저장**되도록 연동(이전엔 파일 선택만 되고 서버로 전송되지 않아 관리자에 '미첨부'로 표시됨)
- 파일은 **비공개 저장소**에 보관, 관리자 회원상세에서 **만료되는 열람 링크**로만 확인 가능(외부 유출 방지)
- 이미지(jpg/png/webp)·PDF, 10MB 이하만 첨부 가능

**관리자 — 대시보드 신설**
- 관리자 첫 화면(`/admin`)을 **운영 요약 대시보드**로 교체(기존에는 회원 화면으로 바로 이동)
- **처리 대기 카드**: 사업자 승인 대기 · 입금 대기 · 배송 처리 대기 · 세금계산서 발행 대기 — 대기 건이 있으면 붉게 강조, 클릭하면 해당 처리 화면으로 바로 이동
- **참고 카드**: 최근 7일 신규 가입 · 유효 견적서 수
- 좌측 메뉴 맨 위에 '대시보드' 추가

**관리자 — 견적서 관리 (발행 견적 조회)**
- 관리자에 **'견적서 관리'** 화면 신설 — 발행된 견적서 목록(견적번호·상호·담당자 검색, 유효/만료 필터, 페이지네이션)과 상세(공급받는 자·품목·금액·유효기간)
- 조회 위주(발행본은 수정 불가). 어떤 회원이 발행했는지도 함께 표시

**관리자 — 주문 관리 (입금확인 · 배송 처리 · 세금계산서 발행)**
- 관리자에 **'주문 관리'** 화면 신설 — 주문 목록(상태 필터·주문번호/주문자/입금자 검색·페이지네이션)과 상세
- 주문 상세에서 **입금확인 → 배송준비 → 배송시작(택배사·운송장 입력) → 배송완료** 순으로 처리. 각 단계는 이전 단계에서만 넘어갈 수 있게 검증
- 세금계산서 신청 건은 입금확인 후 **'발행완료' 처리** 가능(발행일 기록)
- 손님 마이페이지의 주문 상태·운송장·세금계산서 발행 여부가 **이 처리 결과를 그대로** 반영 — 이로써 "주문 → 입금확인 → 배송"의 결제 흐름이 운영상 닫힘

**주문/결제를 서버(DB)에 저장 — 무통장입금 접수**
- 그동안 주문도 **브라우저(localStorage)에만** 저장돼 다른 기기에서 안 보이고 브라우저를 비우면 사라졌음 → 이제 **서버 DB에 계정별로 보관**
- **주문(결제)은 로그인 필요.** 무통장입금으로 접수되며 상태는 **입금대기**로 시작, 세금계산서 신청 시 함께 접수됨(입금확인 후 발행 대기)
- 주문 시 **가격은 서버가 회원 등급 기준으로 다시 계산**해 저장(화면 조작 방지), 주문번호도 서버가 채번
- 마이페이지 **결제내역·세금계산서 내역·배송조회가 실제 저장된 상태**를 표시 — 이전의 "시간이 지나면 자동으로 배송완료되던 데모 동작"을 제거. 배송상태·운송장은 앞으로 **관리자가 처리한 값**을 보여줌
- 개발용 시드에서 주문·견적 목업(localStorage)은 제거

## 2026-07-19

**견적서를 서버(DB)에 저장 — 계정에 귀속, 기기 바뀌어도 유지**
- 그동안 견적서는 **브라우저(localStorage)에만** 저장돼 다른 기기·브라우저에서는 볼 수 없고 브라우저를 비우면 사라졌음 → 이제 **서버 DB에 계정별로 보관**되어 어디서 로그인해도 견적 내역이 그대로 보임
- **견적서 발행은 로그인 필요**(계정에 귀속되는 문서이므로). 비로그인 상태로 발행을 누르면 로그인 화면으로 안내
- 발행 시 **가격은 서버가 회원 등급 기준으로 다시 계산**해 저장 — 화면 조작으로 금액이 바뀐 견적이 저장되지 않도록 함
- **유효기한을 발행 시점에 고정(스냅샷)** — 이후 관리자가 유효기간 설정을 바꿔도 이미 발행된 견적서의 유효기한·만료표시는 발행 당시 그대로 유지
- 개발용 시드에서 견적서 목업(localStorage)은 제거(견적은 이제 실제 로그인 후 발행)

**사이트 설정 — 공급자·계좌·유효기간·고객센터를 관리자 화면에서 편집**
- 그동안 **코드에 하드코딩**돼 있던 값들(공급자 상호·대표·사업자번호·주소·연락처, 무통장입금 계좌, 견적서 유효기간, 고객센터 이메일·전화)을 **관리자 '사이트 설정' 화면에서 직접 편집**하도록 변경 — 값 하나 바꾸는 데 개발·배포가 필요했던 것을 없앰
- 저장하면 **견적서·체크아웃(무통장입금 안내)·계정찾기** 화면에 즉시 반영됨. 견적서 유효기간을 바꾸면 견적서의 유효기한 표기·만료 판정에도 함께 적용
- 최초 값은 기존 값 그대로 옮겨와 **화면 변화 없이** 시작 (실제 사업자 정보로 교체는 이 화면에서)

**관리자 — 회원 등급·정지 기능 추가**
- **회원 등급** 도입: 일반(소비자가) / 회원도매가 2단계. 상품 상세에서 회원도매가가 보이는 자격을 **등급으로 관리** — 승인 사업자는 승인 시 자동으로 회원도매가 등급이 되고, 관리자가 **개인회원에게도 도매가를 부여하거나, 특정 회원의 도매가를 회수**할 수 있음
- **회원 정지/해제**: 활성 회원을 정지하면 **로그인·이용이 차단**되고, 해제하면 원래대로 복구(등급도 유지). 정지 시 사유·처리일이 기록되어 상세에서 확인 가능
- 회원 목록에 **'정지' 필터**와 도매 등급 표시, 상세에 **등급 뱃지·관리 버튼**(등급 부여/회수·정지/해제) 추가
- ※ 도매가 판정 기준을 '승인 사업자'에서 '**회원도매가 등급 + 활성 상태**'로 확장 — 기존 승인 사업자는 자동 이관되어 가격 변화 없음

**회원도매가 노출 정책 정리 — 승인 사업자에게만 표시**
- 이전에는 상품 상세에서 **비회원·개인회원에게도 '회원도매가' 줄이 그대로 노출**되었고, 그 옆 '소비자가'는 실제 값이 아니라 도매가에서 **15%를 역산한 임시 숫자**였음(개발용 mock)
- 이제 **소비자가는 실제 등록값**을 그대로 쓰고, **회원도매가 줄은 승인 사업자(로그인)에게만** 표시. 비회원·개인회원은 소비자가만 봄
- 실제로 장바구니·견적에 담기는 금액은 이전에도 자격에 맞게(승인 사업자=도매가) 정확히 계산되고 있었음 — 이번 변경은 **화면 표시**를 정책에 맞춘 것
- 임시 역산 로직(`getConsumerPrice`) 제거

**관리자 — 회원 관리 확장 (전체 목록·검색·상세)**
- 기존 '회원 승인'(사업자 승인 대기만 보이던 화면)을 **'회원 관리'로 확장** — 개인·사업자 **전체 회원**을 조회
- **검색**(이메일·상호·사업자등록번호)과 **필터**(유형: 개인/사업자, 상태: 활성/승인대기/반려/탈퇴), **페이지네이션**(20명 단위) 추가
- **회원 상세 페이지** 신설 — 이름·연락처·가입일, 사업자 정보(등록번호·상호·대표자·등록증·승인 이력·반려 사유)를 한 화면에서 확인. 승인 대기 사업자는 상세/목록 양쪽에서 바로 승인·반려 가능
- ※ 회원 **정지/해제**와 **등급** 기능은 저장 항목(DB) 추가가 필요해 다음 단계로 분리

**관리자 — 홈 노출 편성(진열대) 기능 추가**
- 관리자 상품 관리에 **'홈 노출 편성'** 화면 신설 — 메인 화면 카테고리별 진열대에 **어떤 상품을 어떤 순서로** 보여줄지 클릭으로 편성. 위로 올릴수록 앞에 진열되며 **카테고리당 최대 8개**
- 이전에는 이 진열 목록이 **파일(featured.json)에 고정**돼 있어 바꾸려면 개발자가 손대야 했음 — 이제 관리자가 직접 편성·저장
- 상품 목록 상단에 **'홈 노출 편성' 버튼** 추가
- **비활성(숨김) 상품은 홈 진열에서 자동 제외** — 진열에 넣어둔 상품을 나중에 비활성으로 바꾸면 홈에서 사라짐(편성 화면에는 '비활성' 표시와 함께 남아 정리 가능)
- 상품 관리 안내 문구 정리(‘등록·수정은 다음 단계’ → 실제 기능 반영)

## 2026-07-16

**타이포그래피 전면 정리 — 얇고 고급스러운 톤**
- 본문 서체를 **Pretendard**로 교체 (자체 호스팅). 한글 서체 특성상 글자 범위별로 쪼개 두어, 페이지마다 **필요한 부분만 내려받는다**
- 사이트 전반의 **글자 굵기를 한 단계씩 낮춤** — 상품명·라벨·내비게이션은 보통 굵기로, 가격·소제목은 중간 굵기로
- 대신 최상단 대형 **Products** 타이틀만 굵게 남겨, "큰 제목 하나만 세우고 나머지는 평평하게" 라는 레퍼런스의 위계를 적용
- 앞으로 굵기 조절은 **한 곳(`globals.css`)의 값 세 개**로 사이트 전체에 반영됨

**메인 카테고리 표기**
- 메인페이지 카테고리 제목에 **한글 아래 영문명 병기** (예: 하드웨어 / Hardware) — 상단 내비와 같은 영문명 사용

**상품 목록 정리**
- 상품 카드·목록에서 **[오늘출발] 평일 14시 마감 표기 삭제** (카드형·목록형 모두)
- 상세페이지 '배송' 안내의 당일출발 설명은 그대로 유지

**견적서를 문서로 승격 — 작성 중 / 발행 이력 분리**
- 견적서 하단에 **'견적서 발행' 버튼** 추가 — 누르면 그 시점 내용 그대로 **견적번호·발행일이 확정**되어 저장됨
- 마이페이지에 **'견적서 내역' 메뉴** 신설 — 발행한 견적서를 최신순으로 보관, 다시 열어 인쇄하거나 주문으로 전환 가능
- 발행본은 **수정 불가(읽기 전용)**. 발행 당시의 가격·품목이 그대로 보존되어, 고객에게 보낸 문서와 나중에 열어본 문서가 항상 같음
- 유효기간이 지난 견적서는 **'유효기간 만료'로 표시**
- ※ 이전에는 견적서를 열 때마다 견적번호와 발행일이 새로 생성되어, 고객에게 보낸 견적서를 다시 찾거나 대조할 수 없었음 — 이번 작업으로 해결

**버그 수정**
- 마이페이지 결제내역·견적서 내역 목록 왼쪽에 **불필요한 목록 점(•)이 보이던 문제 수정**
- 헤더의 **로고·검색·견적서·계정·로그인이 클릭되지 않던 문제 수정** — 화면 상단에 고정된
  카테고리 알약의 투명 영역이 화면 전폭을 덮어 같은 높이의 로고·아이콘 클릭을 가로채고 있었음
  (헤더 바를 없애고 로고·아이콘을 셸 격자에 직접 배치한 이후 발생)
- 마이페이지 **결제내역의 일부 상품 썸네일이 깨지던 문제 수정** — 개발용 시드 데이터가 이미지
  경로를 복제해두어 상품 이미지 파일이 바뀐 뒤 옛 경로에 남아 있었음. 시드가 상품 데이터에서
  이미지를 직접 참조하도록 바꿔 같은 문제가 재발하지 않게 함
  - ※ 이미 주입해 둔 목업 데이터에는 옛 경로가 남아 있으므로 `/dev/seed`에서 **다시 시드**해야 반영됨

## 2026-07-15

**아이콘 · 톤 정리**
- 헤더(검색·견적서·계정·로그인)와 상세설명 아코디언의 **+/− 아이콘을 얇게** 조정 — 선 굵기를 낮춰 전체적으로 가벼운 톤으로 통일
- 상품 카드의 **견적서 바로 추가 아이콘 축소**(34→27px), 아이콘 자체도 작게

**상품 상세**
- 수량을 **직접 입력** 가능하도록 변경 — 테두리 없이 얇은 +/− 아이콘과 숫자만 노출(아코디언과 같은 톤)
- 상세 모달에서 첫 **색상 스와치의 선택 링이 잘리던 문제** 수정

**견적서 패널**
- 패널 외곽을 **위에서 아래로 옅어지는 그라데이션 1px 링**으로 변경
- 헤더를 검정 배경에서 **밝은 회색 면**으로 변경 — 목록과 톤 차이만으로 구분
- 내부 구분선을 **양 끝으로 사라지는 헤어라인**으로 변경, 선 색도 한 단계 옅게
- 모바일 바텀시트에서는 화면 폭을 꽉 채우므로 **외곽 링을 숨김**(내부 구분선은 유지)

**상품 목록 · 카드**
- 레퍼런스처럼 **상품 그리드 배경만 흰색 판**으로 분리 — 카테고리명 열은 회색 배경 유지
- 카드 마우스오버 **그림자를 25% 축소하고 농도도 옅게** 조정
- 카드 **행 간격을 좁힘**(32→26px). 열 간격은 마우스오버 패널이 옆 카드를 침범하지 않는 하한선이라 20px 유지

**상품 · 이미지**
- 수전 카테고리에 **상품 4종 추가** — 새 사진 4장이 목록 앞쪽에 노출
- 수전 대표 이미지를 **전부 흰 배경으로 교체** — 배경이 투명하거나 회색이던 사진 정리

**브랜드**
- 헤더 로고를 **가로형 로고 이미지**로 교체, 기존 심볼+워드마크 조합 제거

**헤더 · 내비게이션 개편**
- 카테고리 내비 바를 **반투명 유리(프로스티드 글라스)** 로 변경 — 뒤로 지나가는 상품이 흐릿하게 비침
- **헤더 바를 없앰** — 흰 배경과 경계선을 걷어내 헤더·내비·상품 리스트가 한 화면으로 이어짐
- 로고를 **왼쪽 견적서 열 위**로 이동, 아이콘은 콘텐츠 우측 상단에 배치 (레퍼런스 구조)
- 왼쪽 견적서 열은 **흰색**, 오른쪽은 **회색**이 화면 맨 위부터 아래까지 이어지는 2단 배경
- 헤더가 **스크롤과 함께 사라지고**, 상단에는 카테고리 내비만 남음

---

## 2026-07-14

**상품 상세 · 이미지**
- 상품 상세를 **모달(팝업형 오버레이)** 로 열도록 개선 — 목록 위에 겹쳐 뜨고 배경이 어두워짐. 직접 URL 접근·새로고침 시엔 일반 상세 페이지로 표시(공유·SEO 유지)
- 상세 모달 톤을 **밝은 회색**으로, **X 닫기 버튼**을 우측 상단 코너 원형 버튼으로 조정
- 모달에서 내용이 길어지면(아코디언 펼침 등) **오른쪽 정보 영역만 내부 스크롤** — 전체 페이지·모달이 늘어나지 않음
- 상품 상세 오른쪽에 **배송·상세설명·크기 아코디언**(+/− 펼침) 추가
- 상품 상세 **다중 이미지 갤러리** 추가 — 메인 이미지 아래 썸네일 1행(최대 6장), 클릭 시 전환. 파일명 규칙(`상품id-1`, `-2`…)으로 자동 인식
- `faucet-0001` 상품에 상세 이미지 5장 추가
- 옵션이 하나뿐인 **단독 상품**은 옵션 선택 없이 소비자가·회원도매가·수량만 바로 표시

**상품 목록 · 카드**
- 카드 썸네일 우측 상단에 **견적서 바로 추가 아이콘**(+툴팁) 추가 — 상세에 안 들어가도 바로 담김
- 카드 **테두리 제거**, 마우스오버 시 **그림자로 떠오르는** 효과로 변경. 추가 아이콘도 그림자 스타일로 통일
- 상품 그리드 **열 수 자동 조정** + 페이지 최대폭 확대(1200→1600px) — 맥북에어에서 4열 노출
- 홈 카테고리 섹션을 좌측 카테고리명 + 우측 그리드 **2단 레이아웃**으로 변경

**내비게이션 · 레이아웃**
- 상단 카테고리 내비 개편 — '전체' 칩 제거, **흰색 알약(pill)형 바**, 마우스오버 시 **영문↔한글 전환**
- 오른쪽 콘텐츠 영역 **흐린 회색 배경**, 메인 최상단 대형 **Products** 타이틀 추가
- 브랜드 심볼을 **파비콘**(주소창 아이콘)으로 적용

**회원 · 로그인**
- 회원가입을 **개인/사업자 유형 선택** 구조로 분리 (`/signup` → `/signup/personal`, `/signup/business`), 헤더 버튼 '회원가입'으로 변경
- **이메일을 공용 계정 아이디(primary key)** 로 정리 — 로그인은 이메일 단일 입력, 사업자 가입에도 이메일 추가(사업자등록번호는 확인용)
- 개인회원 가입은 이메일·비밀번호만 (이름은 주문 단계에서 수집)

---

## 2026-07-09
- 홈 롤링 배너, 칩(chip)형 카테고리 내비게이션, 모바일 견적 UX 추가

## 2026-07-08
- 견적-장바구니 기반 MMM MARKET 스토어프론트 초기 구축
- 검색·로그인·가격 등급(소비자가/회원도매가)·색상 옵션·브랜드 로고
- 목록형/카드형 뷰 전환, 상품 요약 표시
- 사업자 회원가입·계정 찾기 안내, 사업자등록번호 로그인 아이디
- 인쇄용 견적서 페이지, 사업자 체크아웃 흐름
- 마이페이지·주문내역, 개발용 시드 도구
- 견적 흐름을 `/quote`로 통합(중복 `/cart` 제거)

## 2026-07-07
- 프로젝트 초기 생성 (Next.js)
